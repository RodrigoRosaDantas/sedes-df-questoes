name: Reconciliar rastreabilidade de lotes já públicos — SUSPENSO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow suspenso. Nenhuma reconciliação será executada automaticamente."
