name: Mapear páginas do caderno SEEDF Eletrotécnica — SUSPENSO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow suspenso. Nenhum mapeamento será executado automaticamente."
