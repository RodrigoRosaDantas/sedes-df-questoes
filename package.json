{
  "name": "sedes-df-questoes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/validate-data.mjs",
    "check": "node --check assets/app.js && npm test"
  }
}
