name: Saneamento editorial das questões publicadas — SUSPENSO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Informar suspensão
        run: echo "Workflow suspenso. Nenhum saneamento editorial automático será executado."
