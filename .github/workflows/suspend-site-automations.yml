name: Suspender automações do site — CONCLUÍDO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Informar estado
        run: echo "As automações do site estão suspensas e somente poderão ser reativadas por alteração explícita no repositório."
