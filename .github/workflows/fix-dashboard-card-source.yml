name: Corrigir fonte do cartão Banco Mestre — SUSPENSO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Informar suspensão
        run: echo "Workflow suspenso. Nenhuma correção, validação ou publicação será executada."
