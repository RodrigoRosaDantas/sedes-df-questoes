{
  "name": "sedes-df-questoes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/validate-data.mjs && node scripts/smoke-test.mjs",
    "check": "node --check assets/app.js && npm test"
  }
}
