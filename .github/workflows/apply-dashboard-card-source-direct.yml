name: Aplicar fonte correta do cartão Banco Mestre — SUSPENSO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Informar suspensão
        run: echo "Workflow suspenso. Nenhuma alteração ou publicação será executada."
